[project]
name = "imokazakhstan"
version = "0.1.0"
description = "HackAlem 2026 — team imokazakhstan"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.40",
    "python-dotenv>=1.0",
    "streamlit>=1.38",
]

[tool.setuptools]
packages = ["core"]
