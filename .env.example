# Optional runtime inference. Leave blank for the offline demo.
OPENAI_API_KEY=
OPENAI_MODEL=

# Optional TypeSafe Jev decision routing. Leave blank to use deterministic rules.
TYPESAFE_API_KEY=

# Optional only when the team uses Brev from this machine.
BREV_API_KEY=

# Case configuration. Confirm with organizers before using for a scored run.
TIMEZONE=UTC
FORECAST_HORIZON_HOURS=48
OBSERVATION_POLICY=frozen_jan31
